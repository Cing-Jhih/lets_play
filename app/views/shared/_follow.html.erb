<% if current_user %>
  <% if user != current_user %>
    <% if current_user.following?(user)%>
      <%= link_to '取消關注', followship_path(user), method: :delete, class:"btn btn-outline-primary btn-pill" %>
    <% else %>
      <%= link_to '加入關注', followships_path(following_id: user), method: :post, class:"btn btn-primary btn-pill"  %>
    <% end %>
  <% end %>
<% else %>
  <%= link_to '加入關注', followships_path(following_id: user), method: :post, class:"btn btn-primary btn-pill" %>
<% end %>
