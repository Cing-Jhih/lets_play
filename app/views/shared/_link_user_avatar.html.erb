<%= link_to user_path(user) do %>
  <% if user.avatar? %> 
    <%= image_tag user.avatar, width: "50px", class:"rounded img-circle" %>
  <% else %>
    <i class="fa fa-user-edit" aria-hidden="true"></i>
  <% end %>
  <%= link_to user.name, user_path(user) %>
<% end %>