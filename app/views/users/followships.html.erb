<br><br>
<!-- 顯示我關注的人的清單 -->
<% if @followings.present? %>
  <section>
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <div class="sectionTitle text-center ">
            <h2 class="wow fadeInUp">
              <span class="shape shape-left bg-color-4"></span>
              <!-- speech interactive buttons -->
                <span><%= @user == current_user ? '我' : @user.name %>關注的人</span>
              <span class="shape shape-right bg-color-4"></span>
            </h2>
            <%= render partial: "shared/user_intro_list", locals:{ users: @followings}  %>
          </div>
        </div>
      </div>
    </div>
  </section>
<% end %>

<!-- 顯示關注我的人的清單 -->
<% if @followers.present? %>
  <section>
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <div class="sectionTitle text-center ">
            <h2 class="wow fadeInUp">
              <span class="shape shape-left bg-color-4"></span>
              <!-- speech interactive buttons -->
                <span>關注<%= @user == current_user ? '我' : @user.name %>的人</span>
              <span class="shape shape-right bg-color-4"></span>
            </h2>
            <%= render partial: "shared/user_intro_list", locals:{ users: @followers}  %>
          </div>
        </div>
      </div>
    </div>
  </section>
<% end %>
