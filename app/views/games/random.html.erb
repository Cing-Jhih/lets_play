Game title:<%= @game.title %>
<hr>
<% @game.situation_games.each do |situation_game| %>
  <%= situation_game.situation.condition %>
<% end %>  
<hr>
<% @game.age_games.all.order(age_id: :asc).each do |age_game| %>
<%# Why can not set @age_games = @age.age_games.all.order(age: :asc) in controller?  %>
  <%= age_game.age.name %>
<% end %>  
<hr>
<%= link_to 'Random Again', random_games_url(params[:age_game][:age_id]) %>
<%# can't reload with existing params  %>